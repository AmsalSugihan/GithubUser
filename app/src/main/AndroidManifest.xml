<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amsal.githubuser">

    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="com.amsal.githubuser.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.amsal.githubuser.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".provider.GithubUserProvider"
            android:authorities="com.amsal.githubuser"
            android:enabled="true"
            android:readPermission="com.amsal.githubuser.READ_DATABASE"
            android:writePermission="com.amsal.githubuser.WRITE_DATABASE"
            android:exported="true"/>

        <receiver
            android:name=".manager.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".AlarmActivity" />
        <activity android:name=".UsersFavActivity" />
        <activity android:name=".DetailUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>